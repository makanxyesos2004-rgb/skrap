# SoundWave - Music Streaming App

Современное веб-приложение для стриминга музыки с SoundCloud, построенное на React, TypeScript и tRPC.

## 🚀 Быстрый старт

### Требования

- **Node.js** >= 18.0.0
- **pnpm** >= 8.0.0 (рекомендуется) или **npm** >= 9.0.0
- **MySQL** >= 8.0 или **TiDB**

### Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd soundcloud-music-app
   ```

2. **Установите зависимости**
   ```bash
   pnpm install
   ```
   или
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   
   Создайте файл `.env` в корне проекта:
   ```env
   DATABASE_URL=mysql://user:password@localhost:3306/soundwave
   JWT_SECRET=your-secret-key-here
   VITE_APP_ID=soundwave-app
   NODE_ENV=development
   ```

4. **Запустите миграции базы данных**
   ```bash
   pnpm db:push
   ```

5. **Запустите dev сервер**
   ```bash
   pnpm dev
   ```

   Приложение будет доступно по адресу: `http://localhost:3000`

## 📦 Зависимости

Все зависимости перечислены в `requirements.txt` и `package.json`.

### Основные технологии

- **Frontend**: React 19, TypeScript, Vite
- **Backend**: Express, tRPC
- **Database**: MySQL/TiDB с Drizzle ORM
- **UI**: Radix UI, Tailwind CSS
- **State**: TanStack Query

## 🛠️ Доступные команды

```bash
# Разработка
pnpm dev              # Запуск dev сервера с hot reload

# Сборка
pnpm build            # Сборка production версии

# Запуск production
pnpm start            # Запуск production сервера

# База данных
pnpm db:push          # Применить миграции

# Проверка кода
pnpm check            # Проверка TypeScript
pnpm format           # Форматирование кода
pnpm test             # Запуск тестов
```

## 📁 Структура проекта

```
soundcloud-music-app/
├── client/              # Frontend приложение
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── pages/       # Страницы приложения
│   │   ├── contexts/    # React контексты
│   │   └── lib/         # Утилиты
├── server/              # Backend приложение
│   ├── _core/          # Ядро сервера
│   ├── routers.ts      # tRPC роутеры
│   └── soundcloud.ts   # SoundCloud API клиент
├── shared/              # Общий код
├── drizzle/             # Миграции БД
└── package.json         # Зависимости
```

## 🔐 Авторизация

Приложение использует упрощенную систему авторизации для демо:
- Введите любой email на странице `/login`
- Пароль не требуется (только для разработки)

## 🎵 Функции

- ✅ Стриминг музыки с SoundCloud
- ✅ Персональные рекомендации
- ✅ Плейлисты
- ✅ История прослушивания
- ✅ Избранные треки
- ✅ Поиск треков
- ✅ Адаптивный дизайн
- ✅ Мобильный плеер

## 📝 Лицензия

MIT

